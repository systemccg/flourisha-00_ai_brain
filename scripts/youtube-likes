#!/bin/bash
# Wrapper script to run YouTube likes export with virtual environment

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
VENV_PYTHON="$SCRIPT_DIR/.venv/bin/python3"

"$VENV_PYTHON" "$SCRIPT_DIR/youtube_export_likes.py" "$@"
